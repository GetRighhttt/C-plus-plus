#include <iostream> // necessary for all c++ programs
using namespace std; // tells the compiler we're using the standard c++ library

int main() {
    int thisNumber = 1;

    cout << thisNumber << " That's what I thought! You better run when I say so!" << endl;
   return 0;
}